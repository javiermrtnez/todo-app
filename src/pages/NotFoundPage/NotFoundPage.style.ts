import styled from 'styled-components';

const SCNotFoundPage = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20px;

  .vertical-separator {
    height: 45px;
    border-right: 1px solid rgba(255, 255, 255, 0.3);
  }
`;

export default SCNotFoundPage;
