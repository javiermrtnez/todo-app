import { AppIcon } from '../../components/Icons/Icons';
import SCLoadingPage from './LoadingPage.style';

const LoadingPage = () => {
  return (
    <SCLoadingPage>
      <AppIcon />
    </SCLoadingPage>
  );
};

export default LoadingPage;
